platform: linux

image_resource:
  type: docker-image
  source:
    e-mail: ldangelo@mac.com
    repository: hub.docker.com/ldangelo/elixir-phoenix
    tag: 1.0

inputs:
  - name: resource-socialshare

run:
  path: pipeline/build.sh 
  dir: resource-socialshare
