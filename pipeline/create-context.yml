platform: linux
image_resource:
  type: docker-image
  source:
    repository: busybox

inputs:
  - name: socialshare
  - name: _build

outputs:
  - name: context

run:
  path: pipeline/create-context.sh
  dir: socialshare
  
